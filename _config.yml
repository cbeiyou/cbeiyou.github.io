# Fallback config for GitHub Pages' default Jekyll builder.
# This repository is generated by Hugo; when Pages is accidentally set to
# "Deploy from a branch", point Jekyll at prebuilt static files in /public
# to avoid parsing Hugo sources under /content and /themes.
source: public
destination: _site
plugins: []
